import ConfigStore from "../../lib/ConfigStore";
import GDriveStore from "../../lib/GDriveStore";
import {useState} from "react";

export default function FightConfig({}) {

    return (
        <div>
        </div>
    )

}